[package]
name = "zap-plugin-windows"
version = "0.1.0"
edition = "2021"

[dependencies]
zap-core = { path = "../zap-core" }
parking_lot = "0.12"
anyhow = "1"
log = "0.4"

[target.'cfg(target_os = "linux")'.dependencies]
x11rb = { version = "0.13", features = ["allow-unsafe-code"] }
