[package]
name = "zap-plugin-websearch"
version = "0.1.0"
edition = "2021"

[dependencies]
zap-core = { path = "../zap-core" }
serde = { version = "1", features = ["derive"] }
anyhow = "1"
log = "0.4"
percent-encoding = "2"
